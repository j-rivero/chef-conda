---
driver:
  name: exec

transport:
  name: exec

provisioner:
  name: chef_zero
  deprecations_as_errors: true

platforms:
  - name: windows-latest

suites:
  - name: default
    run_list:
      - recipe[chef-conda]
    verifier:
      inspec_tests:
        - test/integration/installation
    attributes:
